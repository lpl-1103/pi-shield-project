# 樹莓派 4 上的 Python 依賴（系統 python3，沒有用 venv）
#
# 版本是 2026-08-12 實機上跑的版本，不是「最低需求」。
# 安裝：pip3 install -r requirements.txt --break-system-packages
#
# ⚠ 另外還有三個「不是 pip 裝的」相依，缺了系統會壞：
#     mpv         apt install mpv          播放器，靠 IPC socket 控制
#     yt-dlp      pip 或官方 binary        解析 YouTube；**要定期更新**，
#                                          YouTube 一改前端就會整個抓不到歌
#     ngrok       官方 binary              對外通道
#
# GPIO 那條（pi3_control.py）用的是 bcm2835 C 函式庫，見 pi3_basic/。

Flask==3.1.1
requests==2.32.3
yt-dlp==2025.4.30
