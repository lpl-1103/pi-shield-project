# Claude Code tooling (not part of the project)
.claude/
.embedder/

# Secrets — never commit these
pi3_line_config.json
projects/pi3-karaoke/**/pi3_line_config.json
*.pem
*.key

# OS / editor cruft
.DS_Store
__pycache__/
*.pyc

# Python 虛擬環境（venv 自己會產生 .venv/.gitignore，但不要依賴那個）
.venv/
venv/

# Empty/placeholder files
code,ipynb

# Compiled vendor example binary (not source)
projects/pi3-karaoke/pi3_basic/examples/blink/pir


# 驗貨站有自己的 git repo，不要被外層 repo 當成 gitlink 追蹤
# （加進來的話 clone 外層只會拿到一個空目錄）
projects/am62a-inspection-station/
